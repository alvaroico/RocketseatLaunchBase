{% extends "layout.njk" %}

{% block content %}

<section class="shop container">
  <h2>Últimos Produtos</h2>
  <div class="items">
    {%for product in products%}
      <div class="product">
        <div class="image"
          style="background: url({{product.img if product.img else 'http://placehold.it/500x500x?text=PRODUTO SEM FOTO'}})"
        >
          {% if product.oldPrice != product.price%}
            <div class="old-price">{{product.oldPrice}}</div>
          {%endif%}
          <p>{{product.price}}</p>
          <h4>{{product.name}}</h4>

          <a href="/products/{{product.id}}" class="button">Ver Produto</a>
        </div>

      </div>
    {%endfor%}
        <h1>teste{{product.price}}</h1>

  </div>
</section>

{% endblock content %}